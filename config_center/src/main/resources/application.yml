spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:dev/,pro/,test/
  application:
    name: config-server
  profiles:
    active: native

server:
  port: 7006

  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
